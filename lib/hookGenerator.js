export function generateHooks(topic){

    return [
      `Stop doing this with ${topic}`,
      `Nobody tells you this about ${topic}`,
      `The biggest mistake in ${topic}`,
      `3 secrets about ${topic}`,
      `Why your ${topic} is failing`,
      `The truth about ${topic}`,
      `This changed my ${topic} forever`,
      `Most people get ${topic} wrong`,
      `Do this before starting ${topic}`,
      `The hidden rule of ${topic}`
    ]
  
  }