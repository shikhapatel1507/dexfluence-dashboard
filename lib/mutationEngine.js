export function mutateHook(hook){

  return [
    hook,
    "WARNING: " + hook,
    "Most people ignore this: " + hook,
    "The biggest mistake: " + hook,
    "Nobody tells you this: " + hook,
    hook + " (watch till the end)",
    "If you do this, stop: " + hook
  ]

}